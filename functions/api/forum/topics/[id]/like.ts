import type { AuthUser } from '../../../../_lib/auth'; import { forumTopicVisible } from '../../../../_lib/forum-notifications'; import { jsonResponse } from '../../../../_lib/response';
interface Env{DB:D1Database} type AuthData=Record<string,unknown>&{user:AuthUser|null};
export const onRequestPut:PagesFunction<Env,any,AuthData>=async({env,data,params})=>{if(!data.user)return jsonResponse({success:false,error:'Silakan login.'},401);const topic=await env.DB.prepare('SELECT * FROM forum_topics WHERE id=? AND deleted_at IS NULL').bind(String(params.id)).first<any>();if(!topic||!(await forumTopicVisible(env.DB,data.user,topic)))return jsonResponse({success:false,error:'Topik tidak ditemukan.'},404);await env.DB.prepare('INSERT OR IGNORE INTO forum_topic_likes (topic_id,user_id) VALUES (?,?)').bind(String(params.id),data.user.id).run();return jsonResponse({success:true})};
export const onRequestDelete:PagesFunction<Env,any,AuthData>=async({env,data,params})=>{if(!data.user)return jsonResponse({success:false,error:'Silakan login.'},401);await env.DB.prepare('DELETE FROM forum_topic_likes WHERE topic_id=? AND user_id=?').bind(String(params.id),data.user.id).run();return jsonResponse({success:true})};
